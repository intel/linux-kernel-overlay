From a5cfab2da7ec1ad1aa78ece5cf3667a18ac39f9f Mon Sep 17 00:00:00 2001
From: Ranjan Dutta <ranjan.dutta@intel.com>
Date: Wed, 10 Mar 2021 12:16:20 +0800
Subject: [PATCH 06/55] Revert "drm/i915: Reject 446-480MHz HDMI clock on GLK"

This reverts commit c51f98210ac685a686e53ad57e029ecebd1f99b6.
---
 drivers/gpu/drm/i915/display/intel_hdmi.c | 6 +-----
 1 file changed, 1 insertion(+), 5 deletions(-)

diff --git a/drivers/gpu/drm/i915/display/intel_hdmi.c b/drivers/gpu/drm/i915/display/intel_hdmi.c
index 1d616da4f165..3f2008d845c2 100644
--- a/drivers/gpu/drm/i915/display/intel_hdmi.c
+++ b/drivers/gpu/drm/i915/display/intel_hdmi.c
@@ -2216,11 +2216,7 @@ hdmi_port_clock_valid(struct intel_hdmi *hdmi,
 					  has_hdmi_sink))
 		return MODE_CLOCK_HIGH;
 
-	/* GLK DPLL can't generate 446-480 MHz */
-	if (IS_GEMINILAKE(dev_priv) && clock > 446666 && clock < 480000)
-		return MODE_CLOCK_RANGE;
-
-	/* BXT/GLK DPLL can't generate 223-240 MHz */
+	/* BXT DPLL can't generate 223-240 MHz */
 	if (IS_GEN9_LP(dev_priv) && clock > 223333 && clock < 240000)
 		return MODE_CLOCK_RANGE;
 
-- 
2.25.1

