From dc070b5216981ce83937a1795aa32b949282d3c6 Mon Sep 17 00:00:00 2001
From: Ranjan Dutta <ranjan.dutta@intel.com>
Date: Wed, 16 Feb 2022 00:20:12 +0800
Subject: [PATCH 19/50] Revert "drm/etnaviv: limit submit sizes"

This reverts commit 605583fcccb51c73362f5c2b326693da692e4492.
---
 drivers/gpu/drm/etnaviv/etnaviv_gem_submit.c | 6 ------
 1 file changed, 6 deletions(-)

diff --git a/drivers/gpu/drm/etnaviv/etnaviv_gem_submit.c b/drivers/gpu/drm/etnaviv/etnaviv_gem_submit.c
index ed2c50011d44..5f24cc52c287 100644
--- a/drivers/gpu/drm/etnaviv/etnaviv_gem_submit.c
+++ b/drivers/gpu/drm/etnaviv/etnaviv_gem_submit.c
@@ -469,12 +469,6 @@ int etnaviv_ioctl_gem_submit(struct drm_device *dev, void *data,
 		return -EINVAL;
 	}
 
-	if (args->stream_size > SZ_64K || args->nr_relocs > SZ_64K ||
-	    args->nr_bos > SZ_64K || args->nr_pmrs > 128) {
-		DRM_ERROR("submit arguments out of size limits\n");
-		return -EINVAL;
-	}
-
 	/*
 	 * Copy the command submission and bo array to kernel space in
 	 * one go, and do this outside of any locks.
-- 
2.27.0

