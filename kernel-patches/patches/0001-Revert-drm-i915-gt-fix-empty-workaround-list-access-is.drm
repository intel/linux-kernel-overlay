From 256e54e147fe2232616186deb21e54d86e7a29e7 Mon Sep 17 00:00:00 2001
From: Junxiao Chang <junxiao.chang@intel.com>
Date: Thu, 6 Jul 2023 15:01:13 +0800
Subject: [PATCH 0001/2351] Revert "drm/i915/gt: fix empty workaround list
 access issue"

This reverts commit d59eca0d205fcba7f3a8f6f18f840c83d5dfa426.
---
 drivers/gpu/drm/i915/gt/intel_workarounds.c | 4 +---
 1 file changed, 1 insertion(+), 3 deletions(-)

diff --git a/drivers/gpu/drm/i915/gt/intel_workarounds.c b/drivers/gpu/drm/i915/gt/intel_workarounds.c
index 7a404e5bf9b08..a6e6c906dba26 100644
--- a/drivers/gpu/drm/i915/gt/intel_workarounds.c
+++ b/drivers/gpu/drm/i915/gt/intel_workarounds.c
@@ -1826,7 +1826,7 @@ wa_verify(struct intel_gt *gt, const struct i915_wa *wa, u32 cur,
 static void wa_list_apply(const struct i915_wa_list *wal)
 {
 	struct intel_gt *gt = wal->gt;
-	struct intel_uncore *uncore;
+	struct intel_uncore *uncore = gt->uncore;
 	enum forcewake_domains fw;
 	unsigned long flags;
 	struct i915_wa *wa;
@@ -1835,8 +1835,6 @@ static void wa_list_apply(const struct i915_wa_list *wal)
 	if (!wal->count)
 		return;
 
-	uncore = gt->uncore;
-
 	fw = wal_get_fw_for_rmw(uncore, wal);
 
 	intel_gt_mcr_lock(gt, &flags);
-- 
2.25.1

