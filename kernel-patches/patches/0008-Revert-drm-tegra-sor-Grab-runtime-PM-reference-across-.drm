From d3f0232ee08246e234685323c084919896a3adff Mon Sep 17 00:00:00 2001
From: Ranjan Dutta <ranjan.dutta@intel.com>
Date: Mon, 19 Apr 2021 09:41:44 +0800
Subject: [PATCH 08/13] Revert "drm/tegra: sor: Grab runtime PM reference
 across reset"

This reverts commit d2308dd5119bd99bf00b7eb2836bd605299cfaf9.
---
 drivers/gpu/drm/tegra/sor.c | 7 -------
 1 file changed, 7 deletions(-)

diff --git a/drivers/gpu/drm/tegra/sor.c b/drivers/gpu/drm/tegra/sor.c
index 7b88261f57bb..f02a035dda45 100644
--- a/drivers/gpu/drm/tegra/sor.c
+++ b/drivers/gpu/drm/tegra/sor.c
@@ -3115,12 +3115,6 @@ static int tegra_sor_init(struct host1x_client *client)
 	 * kernel is possible.
 	 */
 	if (sor->rst) {
-		err = pm_runtime_resume_and_get(sor->dev);
-		if (err < 0) {
-			dev_err(sor->dev, "failed to get runtime PM: %d\n", err);
-			return err;
-		}
-
 		err = reset_control_acquire(sor->rst);
 		if (err < 0) {
 			dev_err(sor->dev, "failed to acquire SOR reset: %d\n",
@@ -3154,7 +3148,6 @@ static int tegra_sor_init(struct host1x_client *client)
 		}
 
 		reset_control_release(sor->rst);
-		pm_runtime_put(sor->dev);
 	}
 
 	err = clk_prepare_enable(sor->clk_safe);
-- 
2.25.1

