From 2b3f81943690f769fdfff0168c5c94e51de358b6 Mon Sep 17 00:00:00 2001
From: Ranjan Dutta <ranjan.dutta@intel.com>
Date: Tue, 27 Jul 2021 00:06:49 +0800
Subject: [PATCH 75/77] Revert "drm/bridge: Fix the stop condition of
 drm_bridge_chain_pre_enable()"

This reverts commit 0cd39c96574d19e234cee971868c82c3769c2ee6.
---
 drivers/gpu/drm/drm_bridge.c | 3 ---
 1 file changed, 3 deletions(-)

diff --git a/drivers/gpu/drm/drm_bridge.c b/drivers/gpu/drm/drm_bridge.c
index 044acd07c153..64f0effb52ac 100644
--- a/drivers/gpu/drm/drm_bridge.c
+++ b/drivers/gpu/drm/drm_bridge.c
@@ -522,9 +522,6 @@ void drm_bridge_chain_pre_enable(struct drm_bridge *bridge)
 	list_for_each_entry_reverse(iter, &encoder->bridge_chain, chain_node) {
 		if (iter->funcs->pre_enable)
 			iter->funcs->pre_enable(iter);
-
-		if (iter == bridge)
-			break;
 	}
 }
 EXPORT_SYMBOL(drm_bridge_chain_pre_enable);
-- 
2.25.1

