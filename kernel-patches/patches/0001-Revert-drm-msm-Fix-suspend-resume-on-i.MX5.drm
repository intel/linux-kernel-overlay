From a95c5089c554bfbd4cb3c0bd4624fe72001b8065 Mon Sep 17 00:00:00 2001
From: Ranjan Dutta <ranjan.dutta@intel.com>
Date: Fri, 9 Apr 2021 20:08:41 +0800
Subject: [PATCH 01/11] Revert "drm/msm: Fix suspend/resume on i.MX5"

This reverts commit 447a011bb40d2cefa6df367b84757c95616d2803.
---
 drivers/gpu/drm/msm/msm_drv.c | 8 --------
 1 file changed, 8 deletions(-)

diff --git a/drivers/gpu/drm/msm/msm_drv.c b/drivers/gpu/drm/msm/msm_drv.c
index b38ebccad42f..45e325c982c2 100644
--- a/drivers/gpu/drm/msm/msm_drv.c
+++ b/drivers/gpu/drm/msm/msm_drv.c
@@ -1079,10 +1079,6 @@ static int __maybe_unused msm_pm_resume(struct device *dev)
 static int __maybe_unused msm_pm_prepare(struct device *dev)
 {
 	struct drm_device *ddev = dev_get_drvdata(dev);
-	struct msm_drm_private *priv = ddev ? ddev->dev_private : NULL;
-
-	if (!priv || !priv->kms)
-		return 0;
 
 	return drm_mode_config_helper_suspend(ddev);
 }
@@ -1090,10 +1086,6 @@ static int __maybe_unused msm_pm_prepare(struct device *dev)
 static void __maybe_unused msm_pm_complete(struct device *dev)
 {
 	struct drm_device *ddev = dev_get_drvdata(dev);
-	struct msm_drm_private *priv = ddev ? ddev->dev_private : NULL;
-
-	if (!priv || !priv->kms)
-		return;
 
 	drm_mode_config_helper_resume(ddev);
 }
-- 
2.25.1

