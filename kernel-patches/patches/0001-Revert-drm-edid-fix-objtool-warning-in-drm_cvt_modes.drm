From 7092688ddabe8ed5cee58a663af2bfcbc7db16ac Mon Sep 17 00:00:00 2001
From: Ranjan Dutta <ranjan.dutta@intel.com>
Date: Thu, 7 Jan 2021 11:20:54 +0800
Subject: [PATCH 01/42] Revert "drm/edid: fix objtool warning in
 drm_cvt_modes()"

This reverts commit bb25fd492693a3a8654318f79c4c9e22f2d02aa1.
---
 drivers/gpu/drm/drm_edid.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/gpu/drm/drm_edid.c b/drivers/gpu/drm/drm_edid.c
index b7ddf504e024..b84efd538a70 100644
--- a/drivers/gpu/drm/drm_edid.c
+++ b/drivers/gpu/drm/drm_edid.c
@@ -3102,8 +3102,6 @@ static int drm_cvt_modes(struct drm_connector *connector,
 
 		height = (cvt->code[0] + ((cvt->code[1] & 0xf0) << 4) + 1) * 2;
 		switch (cvt->code[1] & 0x0c) {
-		/* default - because compiler doesn't see that we've enumerated all cases */
-		default:
 		case 0x00:
 			width = height * 4 / 3;
 			break;
@@ -3116,6 +3114,8 @@ static int drm_cvt_modes(struct drm_connector *connector,
 		case 0x0c:
 			width = height * 15 / 9;
 			break;
+		default:
+			unreachable();
 		}
 
 		for (j = 1; j < 5; j++) {
-- 
2.25.1

