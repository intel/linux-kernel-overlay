From 65baaeb2ce2ee5b706aed30e632adf7274070c2b Mon Sep 17 00:00:00 2001
From: Ranjan Dutta <ranjan.dutta@intel.com>
Date: Tue, 27 Jul 2021 00:06:48 +0800
Subject: [PATCH 72/77] Revert "drm/vmwgfx: Mark a surface gpu-dirty after the
 SVGA3dCmdDXGenMips command"

This reverts commit a8e85ed08858d0f34b20e5a8cc8063b2e34e019d.
---
 drivers/gpu/drm/vmwgfx/vmwgfx_execbuf.c | 20 ++++----------------
 1 file changed, 4 insertions(+), 16 deletions(-)

diff --git a/drivers/gpu/drm/vmwgfx/vmwgfx_execbuf.c b/drivers/gpu/drm/vmwgfx/vmwgfx_execbuf.c
index 83e1b54eb864..e67e2e8f6e6f 100644
--- a/drivers/gpu/drm/vmwgfx/vmwgfx_execbuf.c
+++ b/drivers/gpu/drm/vmwgfx/vmwgfx_execbuf.c
@@ -2759,24 +2759,12 @@ static int vmw_cmd_dx_genmips(struct vmw_private *dev_priv,
 {
 	VMW_DECLARE_CMD_VAR(*cmd, SVGA3dCmdDXGenMips) =
 		container_of(header, typeof(*cmd), header);
-	struct vmw_resource *view;
-	struct vmw_res_cache_entry *rcache;
+	struct vmw_resource *ret;
 
-	view = vmw_view_id_val_add(sw_context, vmw_view_sr,
-				   cmd->body.shaderResourceViewId);
-	if (IS_ERR(view))
-		return PTR_ERR(view);
+	ret = vmw_view_id_val_add(sw_context, vmw_view_sr,
+				  cmd->body.shaderResourceViewId);
 
-	/*
-	 * Normally the shader-resource view is not gpu-dirtying, but for
-	 * this particular command it is...
-	 * So mark the last looked-up surface, which is the surface
-	 * the view points to, gpu-dirty.
-	 */
-	rcache = &sw_context->res_cache[vmw_res_surface];
-	vmw_validation_res_set_dirty(sw_context->ctx, rcache->private,
-				     VMW_RES_DIRTY_SET);
-	return 0;
+	return PTR_ERR_OR_ZERO(ret);
 }
 
 /**
-- 
2.25.1

