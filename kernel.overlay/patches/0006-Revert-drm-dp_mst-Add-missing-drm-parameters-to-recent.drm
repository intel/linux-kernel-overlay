From ae4f246c76a4c5d5bb69510a00017461274c7f7e Mon Sep 17 00:00:00 2001
From: Ranjan Dutta <ranjan.dutta@intel.com>
Date: Tue, 27 Jul 2021 00:06:29 +0800
Subject: [PATCH 06/77] Revert "drm/dp_mst: Add missing drm parameters to
 recently added call to drm_dbg_kms()"

This reverts commit cae871baa4f30608dc2084ca8ddc8f7e49913721.
---
 drivers/gpu/drm/drm_dp_mst_topology.c | 7 ++-----
 1 file changed, 2 insertions(+), 5 deletions(-)

diff --git a/drivers/gpu/drm/drm_dp_mst_topology.c b/drivers/gpu/drm/drm_dp_mst_topology.c
index 861f16dfd1a3..01570f2549e1 100644
--- a/drivers/gpu/drm/drm_dp_mst_topology.c
+++ b/drivers/gpu/drm/drm_dp_mst_topology.c
@@ -3385,9 +3385,7 @@ int drm_dp_update_payload_part1(struct drm_dp_mst_topology_mgr *mgr)
 			mutex_unlock(&mgr->lock);
 
 			if (skip) {
-				drm_dbg_kms(mgr->dev,
-					    "Virtual channel %d is not in current topology\n",
-					    i);
+				drm_dbg_kms("Virtual channel %d is not in current topology\n", i);
 				continue;
 			}
 			/* Validated ports don't matter if we're releasing
@@ -3402,8 +3400,7 @@ int drm_dp_update_payload_part1(struct drm_dp_mst_topology_mgr *mgr)
 						payload->start_slot = req_payload.start_slot;
 						continue;
 					} else {
-						drm_dbg_kms(mgr->dev,
-							    "Fail:set payload to invalid sink");
+						drm_dbg_kms("Fail:set payload to invalid sink");
 						mutex_unlock(&mgr->payload_lock);
 						return -EINVAL;
 					}
-- 
2.25.1

