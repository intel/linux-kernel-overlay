From 5bacec106b93c97dd0058c7bfab3fc6263b782a3 Mon Sep 17 00:00:00 2001
From: Ranjan Dutta <ranjan.dutta@intel.com>
Date: Fri, 9 Apr 2021 20:08:41 +0800
Subject: [PATCH 02/11] Revert "drm/msm: fix shutdown hook in case GPU
 components failed to bind"

This reverts commit c7552dee62a0ae25f55e5b02bb1b4bcdf7eb5519.
---
 drivers/gpu/drm/msm/msm_drv.c | 4 ----
 1 file changed, 4 deletions(-)

diff --git a/drivers/gpu/drm/msm/msm_drv.c b/drivers/gpu/drm/msm/msm_drv.c
index 45e325c982c2..3d0adfa6736a 100644
--- a/drivers/gpu/drm/msm/msm_drv.c
+++ b/drivers/gpu/drm/msm/msm_drv.c
@@ -1318,10 +1318,6 @@ static int msm_pdev_remove(struct platform_device *pdev)
 static void msm_pdev_shutdown(struct platform_device *pdev)
 {
 	struct drm_device *drm = platform_get_drvdata(pdev);
-	struct msm_drm_private *priv = drm ? drm->dev_private : NULL;
-
-	if (!priv || !priv->kms)
-		return;
 
 	drm_atomic_helper_shutdown(drm);
 }
-- 
2.25.1

