From 2aa270a6d56d10c070467dd9cba4cf68a22854e9 Mon Sep 17 00:00:00 2001
From: Jarkko Nikula <jarkko.nikula@linux.intel.com>
Date: Wed, 28 Oct 2020 15:05:51 +0200
Subject: [PATCH 22/61] counter: intel-qep: Add braces to if statement
 according to coding style

Linux kernel coding style says braces should be used in both branches of
a conditional statement if other branch needs them. Like:

        if (condition) {
                do_this();
                do_that();
        } else {
                otherwise();
        }

Fix this in intel_qep_irq_thread().

Signed-off-by: Jarkko Nikula <jarkko.nikula@linux.intel.com>
---
 drivers/counter/intel-qep.c | 6 ++++--
 1 file changed, 4 insertions(+), 2 deletions(-)

diff --git a/drivers/counter/intel-qep.c b/drivers/counter/intel-qep.c
index c24cf7580bfe..3e2e6fd2eb65 100644
--- a/drivers/counter/intel-qep.c
+++ b/drivers/counter/intel-qep.c
@@ -159,10 +159,12 @@ static irqreturn_t intel_qep_irq_thread(int irq, void *_qep)
 		if (qep->op_mode == INTEL_QEP_OP_MODE_QEP) {
 			dev_dbg(qep->dev, "Phase Error detected\n");
 			qep->phase_error = true;
-		} else
+		} else {
 			dev_dbg(qep->dev, "Fifo Critical\n");
-	} else
+		}
+	} else {
 		qep->phase_error = false;
+	}
 
 	if (stat & INTEL_QEPINT_FIFOENTRY)
 		dev_dbg(qep->dev, "Fifo Entry\n");
-- 
2.27.0

