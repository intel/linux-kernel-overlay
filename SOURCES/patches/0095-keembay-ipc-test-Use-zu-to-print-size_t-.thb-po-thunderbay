From 391070d7255c6bee07d32cf100740548963422ec Mon Sep 17 00:00:00 2001
From: Daniele Alessandrelli <daniele.alessandrelli@intel.com>
Date: Fri, 3 Jan 2020 11:52:40 +0000
Subject: [PATCH 095/223] keembay-ipc-test: Use %zu to print size_t variables

We were using %ld to print size_t variables, while the correct format
specified is %zu.

Reported-by: kbuild test robot <lkp@intel.com>
Signed-off-by: Daniele Alessandrelli <daniele.alessandrelli@intel.com>
---
 drivers/misc/keembay-ipc/keembay-ipc-test.c | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/drivers/misc/keembay-ipc/keembay-ipc-test.c b/drivers/misc/keembay-ipc/keembay-ipc-test.c
index 54396b7d21e9..e08d332f30c7 100644
--- a/drivers/misc/keembay-ipc/keembay-ipc-test.c
+++ b/drivers/misc/keembay-ipc/keembay-ipc-test.c
@@ -308,7 +308,7 @@ static ssize_t recv_wr(struct file *filp, const char __user *buf, size_t count,
 	if (rc)
 		pr_info("recv(chan_id=%lu) failed: %d\n", chan_id, rc);
 	else
-		pr_info("recv(chan_id=%lu) paddr: %u\tsize: %ld\n", chan_id,
+		pr_info("recv(chan_id=%lu) paddr: %u\tsize: %zu\n", chan_id,
 			paddr, size);
 
 	return count;
-- 
2.27.0

